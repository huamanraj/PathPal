
    // Set the height of the scrollbar thumb dynamically
    const customScrollbar = document.getElementById('customScrollbar');
    const thumbHeight = 4; // Set the desired height in vh
    customScrollbar.style.paddingRight = `calc(${thumbHeight}vh - 8px)`; // Adjust the calculation based on your preferences
